
<div class="container mx-auto my-8 px-4">
  <div class="max-w-xl mx-auto">
    <h1 class="text-2xl font-bold mb-6">Editing <%= @book.book_title.titleize %></h1>

    <%= render 'form', book: @book %>
  </div>
  <%= link_to 'Show', @book %> |
<%= link_to 'Back', books_path %>
</div>